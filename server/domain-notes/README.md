# 领域素材包（可选）

在这个目录放 `food.md` / `travel.md`，服务启动时会自动加载并注入对应房间 AI 的 system prompt；
文件不存在就跳过。单个文件最多注入 2500 字符（超出截断）。也可在主持人控制台在线编辑。

## 格式示例（food.md）

```markdown
## 口语素材
- "踩坑了" —— 踩雷餐厅

## 经历片段
- 朋友安利的店盐放多了，整桌人都在喝水

## 吐槽
- 点评分数和实际差太远
```

注意：素材会进入 AI 的发言，别放任何涉密/敏感信息。
